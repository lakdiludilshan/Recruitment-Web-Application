function saveJob() {
    // Get the form element
    var saveJobForm = document.getElementById('saveJobForm');
  
    // Submit the form
    saveJobForm.submit();
  
    // Prevent the default form submission behavior
    return false;
  }

  function applyJob() {
    // Get the form element
    var applyJobForm = document.getElementById('applyJobForm');
  
    // Submit the form
    applyJobForm.submit();
  
    // Prevent the default form submission behavior
    return false;
  }